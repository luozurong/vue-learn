/**
 * 空间复杂度
 * 1. 是什么
 * 执行当前算法需要占用多少存储空间
 * 
 * 2.表示法
 * O(1) O(n) O(n^2)...
 */

// O(1)
let a = 1
a++


// O(n)
let n = 100
let arr = []
for(let i = 0; i < n; i++) {
  arr.push([])
  for (let k = 0; k < n; k ++) {
    arr[j].push('a')
  }
}